/* const fs = require('fs');
fs.readFile('quesitons.txt', 'utf8', function(err, data){
    if (err){
        console.error(err);
        return;
    }
    const lines = data.split('\n');
    lines.forEach(function(line){
        console.log(line)
    })
})*/